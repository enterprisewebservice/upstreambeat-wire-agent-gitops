# Marker file — presence of this path tells the agent-office-agents
# ArgoCD ApplicationSet (SCM generator, pathsExist filter) to adopt this
# repo: it creates the Application (<name>-agent) in the shared
# agent-office project. Replaces the scaffolder's old argocd-register
# step (the roadie argocd:create-resources action, which raced on
# per-app project creation). Do not delete.
